{
  "$id": "https://aubaine.io/schema/skill-tree.json",
  "title": "Arbre de compétences",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Identifiant de l'arbre, qui devient son URL."
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "playtest",
        "beta",
        "balanced"
      ],
      "description": "Maturité de l'entrée, de la moins arrêtée à la plus arrêtée. 'draft' est en cours d'écriture et n'est pas encore jouable ; 'playtest', 'beta' et 'draft' portent un badge ; 'balanced' n'en porte aucun mais interrompt l'héritage. Absent : la valeur est héritée de ce qui possède l'entrée, un arbre, une pièce d'équipement ou une panoplie, dans cet ordre."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Titre rendu sur la planche."
    },
    "subtitle": {
      "description": "Devise de l'arbre, rendue en italique sous le titre sur la page et sur la couverture du livret. Absente, le titre reste seul.",
      "type": "string",
      "minLength": 1
    },
    "treeType": {
      "type": "string",
      "enum": [
        "species",
        "archetype",
        "domain"
      ],
      "description": "Rendu en sous-titre : Espèce, Archétype ou Domaine."
    },
    "size": {
      "anyOf": [
        {
          "type": "number",
          "const": 8
        },
        {
          "type": "number",
          "const": 16
        }
      ],
      "description": "Nombre de Compétences que la planche est dessinée pour tenir."
    },
    "cover": {
      "description": "Couverture au rapport 3:4, dans data/media/art/ : la planche verticale que porte la carte de l'arbre. Absente, la carte retombe sur la bannière, puis sur la planche 3:4 par défaut.",
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*-\\d{1,2}_\\d{1,2}-(?:og|cleaned|upscaled_[24])\\.(?:png|jpg)$"
    },
    "banner": {
      "description": "Bannière au rapport 16:9, dans data/media/art/ : le bandeau derrière le héros et la vignette sociale. Absente, le héros retombe sur la planche 16:9 par défaut.",
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*-\\d{1,2}_\\d{1,2}-(?:og|cleaned|upscaled_[24])\\.(?:png|jpg)$"
    },
    "backCover": {
      "description": "Quatrième de couverture au rapport 3:4, dans data/media/art/ : la planche verticale au dos du livret imprimé. Absente, le dos retombe sur la bannière recadrée en portrait.",
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*-\\d{1,2}_\\d{1,2}-(?:og|cleaned|upscaled_[24])\\.(?:png|jpg)$"
    },
    "core": {
      "description": "Cœur de l'arbre. Absent : un nœud tient le centre.",
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "minLength": 1
        },
        "sublabel": {
          "type": "string"
        },
        "domains": {
          "description": "Bordure de l'emblème.",
          "maxItems": 2,
          "type": "array",
          "items": {
            "type": "string",
            "description": "Clé de Domaine, telle qu'elle est déclarée dans data/meta/domains.json."
          }
        },
        "pos": {
          "description": "Défaut : 50, 50.81.",
          "type": "object",
          "properties": {
            "x": {
              "type": "number",
              "minimum": 0,
              "maximum": 100
            },
            "y": {
              "type": "number",
              "minimum": 0,
              "maximum": 100
            }
          },
          "required": [
            "x",
            "y"
          ],
          "additionalProperties": false
        }
      },
      "required": [
        "label"
      ],
      "additionalProperties": false
    },
    "placements": {
      "maxItems": 16,
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "skill": {
            "type": "string",
            "pattern": "^[A-Z0-9]{5}-[0-9]{2}$",
            "description": "Identifiant de la Compétence posée, définie dans data/skills/."
          },
          "pos": {
            "description": "Absent : la Compétence est listée avec l'arbre mais n'a pas de pastille sur la planche.",
            "type": "object",
            "properties": {
              "x": {
                "type": "number",
                "minimum": 0,
                "maximum": 100
              },
              "y": {
                "type": "number",
                "minimum": 0,
                "maximum": 100
              }
            },
            "required": [
              "x",
              "y"
            ],
            "additionalProperties": false
          },
          "linked": {
            "description": "Parents visuels vers lesquels tracer un trait. 'CORE' vise le cœur de l'arbre. Déclaré d'un seul côté : jamais de doublon inverse.",
            "minItems": 1,
            "type": "array",
            "items": {
              "anyOf": [
                {
                  "type": "string",
                  "pattern": "^[A-Z0-9]{5}-[0-9]{2}$"
                },
                {
                  "type": "string",
                  "const": "CORE"
                }
              ]
            }
          }
        },
        "required": [
          "skill"
        ],
        "additionalProperties": false
      },
      "description": "Les Compétences de l'arbre et leur mise en page. L'ordre n'a pas d'effet au rendu. Vide : l'arbre est annoncé mais sa planche est encore nue, ce que seul un brouillon peut être."
    }
  },
  "required": [
    "id",
    "name",
    "treeType",
    "size",
    "placements"
  ],
  "additionalProperties": false
}
